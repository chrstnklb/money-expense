<%= form_with(model: money_expense) do |form| %>
  <% if money_expense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(money_expense.errors.count, "error") %> prohibited this money_expense from being saved:</h2>

      <ul>
        <% money_expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col">
      <%= form.label :price, style: "display: block" %>
      <%= form.number_field :price,
        placeholder: "Auf ganze € aufrunden!",
        style: "width: 304px;",
        type: "number",
        min: 0,
        step: 1,
        pattern: "\\d*" %>
    </div>
  </div>
    
  <div class="row">
    <div class="col">
      <%= form.label :category %>
      <%= form.select :category, options_for_select(MoneyExpense::CATEGORIES, selected: money_expense.category), style: "width: 304px;" %>
    </div>
  </div>
      
  <div class="row">
    <div class="col">
      <%= form.label :comment, style: "display: block" %>
      <%= form.text_field :comment, placeholder: "Kommentiere!", style: "width: 304px;" %>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <%= button_tag "Ausgabe speichern", type: :submit, class: "btn btn-small btn-success", style: "width: 304px;" %>
    </div>
  </div>

<% end %>
